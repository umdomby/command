``` package fuull
using System;
using System.Drawing;
using System.Net.Sockets;
using System.Text;
using System.Windows.Forms;

namespace GridLegendsDSX
{
public partial class Form1 : Form
{
private string targetIp = "127.0.0.1";
private int targetPort = 20778;
private bool isTightMode = false;
private Timer udpTimer;
private int packetCount = 0;
private Label lblCount;
private Button btnOn, btnOff;

        public Form1()
        {
            InitializeComponent();
            this.Text = "DSX: Тест Твердого Курка (UDP Поток)";
            this.Size = new Size(360, 250);
            this.FormBorderStyle = FormBorderStyle.FixedSingle;
            this.StartPosition = FormStartPosition.CenterScreen;

            // Счётчик пакетов
            lblCount = new Label();
            lblCount.Location = new Point(20, 140);
            lblCount.Size = new Size(320, 30);
            lblCount.Text = "Пакетов: 0 | Режим: OFF";
            lblCount.Font = new Font("Arial", 10, FontStyle.Bold);
            this.Controls.Add(lblCount);

            // Кнопка ON
            btnOn = new Button();
            btnOn.Text = "🔴 ТВЁРДЫЙ КУРОК (ON)";
            btnOn.Location = new Point(20, 20);
            btnOn.Size = new Size(300, 45);
            btnOn.BackColor = Color.Gray;
            btnOn.ForeColor = Color.White;
            btnOn.Click += BtnOn_Click;
            this.Controls.Add(btnOn);

            // Кнопка OFF
            btnOff = new Button();
            btnOff.Text = "🟢 МЯГКИЙ КУРОК (OFF)";
            btnOff.Location = new Point(20, 75);
            btnOff.Size = new Size(300, 45);
            btnOff.BackColor = Color.Gray;
            btnOff.ForeColor = Color.White;
            btnOff.Click += BtnOff_Click;
            this.Controls.Add(btnOff);

            // Таймер: 50 мс поток
            udpTimer = new Timer { Interval = 50 };
            udpTimer.Tick += UdpTimer_Tick;
            udpTimer.Start();
        }

        private void BtnOn_Click(object sender, EventArgs e)
        {
            isTightMode = true;
            btnOn.BackColor = Color.Red;
            btnOff.BackColor = Color.Gray;
        }

        private void BtnOff_Click(object sender, EventArgs e)
        {
            isTightMode = false;
            btnOff.BackColor = Color.Green;
            btnOn.BackColor = Color.Gray;
        }

        private void UdpTimer_Tick(object sender, EventArgs e)
        {
            string command;
            if (isTightMode)
            {
                // ✅ Точный формат DSX v3.14a из твоего лога
                command = "RightTrigger=Resistance\r\nForceRightTrigger=(9)(8)\r\n";
            }
            else
            {
                command = "RightTrigger=Normal\r\n";
            }

            SendUDP(command);
            packetCount++;
            lblCount.Text = $"Пакетов: {packetCount} | Режим: {(isTightMode ? "ТВЁРДЫЙ" : "МЯГКИЙ")}";
        }

        private void SendUDP(string command)
        {
            try
            {
                using (var udp = new UdpClient())
                {
                    byte[] data = Encoding.UTF8.GetBytes(command);
                    udp.Send(data, data.Length, targetIp, targetPort);
                }
            }
            catch { } // Тихо
        }
    }
}
```
