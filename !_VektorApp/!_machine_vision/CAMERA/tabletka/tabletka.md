1200 x 110

"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\empty" - пустая ячейка
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_g_no" - серая таблетка поврежденная
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_g_od" - серая таблетка неоднородная
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_g_ok" - серая таблетка хорошая
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_w_no" - белая таблетка поврежденная
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_w_od" - белая таблетка неоднородная
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\tablet_w_ok" - белая таблетка хорошая
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\train\trigger_mark" программный триггер


камера с 100 FPS должна распознавать какие таблетки находятся в блистере
блистер детектим рядами через программный триггер ИИ класса trigger_mark
"C:\_VIDEO\picoCam-303C-I2D303C-RCA11-BLISTER\png\best.onnx"

в консоль выводи информацию о золотом кадре  10 ячейках (6-ю пропускай)

empty
tablet_g_no
tablet_g_od 
tablet_g_ok 
tablet_w_no 
tablet_w_od 
tablet_w_ok 

для сокращения выводи (в каком ряду в какой ячейке что находится)
empty = 0
tablet_g_no = g_no
tablet_g_od = g_od
tablet_g_ok = g_ok
tablet_w_no = w_no
tablet_w_od = w_od
tablet_w_ok = w_ok
когда происходит обнаружение нового ряда делается золотой кадр и в консоле отображается что находится в каждой зоне 
(между 6 и 7 линией в этой зоне ничего определять не нужно, во всех остальных зонах модель должна опредлить что там находится из классов)





класс trigger_mark используй как триггер золотого кадра

код не изменяй - добавь детекцию поиска рядов и делай золотой кадр каждого ряда и в консоль выводи что находится в кажом отсеке каждого ряда,
в консоль выводи тогда когда происходит детекция ряда и данные по каждой ячейки между 6 и 7 пропускай там пустота
с учетом того что конвейер может останавливаться и изменять скорость, детект должен осуществляться каждого ряда без дублирований

Основная проблема в том, что сейчас скрипт работает по принципу «одна ячейка — один запрос к ИИ». На 100 FPS и при 10 ячейках в ряду это 1000 запросов в секунду. зачем? 
ты анализаируй сначала trigger_mark, как он сработал по одному кадру сделай один раз анализ


Чтобы при 100 FPS система не "захлебнулась", мы меняем стратегию: нейросеть в каждом кадре анализирует только одну зону (первую), где должен появиться trigger_mark. Как только метка обнаружена, мы берем этот один кадр, нарезаем его на 10 частей и отправляем их в ИИ одним пакетом (batch inference). Это в 10 раз быстрее, чем обрабатывать их по очереди.

Также я добавил логику защиты от дублей: пока метка не исчезнет из зоны, повторный анализ ряда не начнется (даже если конвейер встал).
trigger_mark
10 зон между 6 и 7 пустая зона ее не учитывай

Один запрос к ИИ на весь кадр: Ищем trigger_mark по всему изображению.
Фиксация момента: Как только ИИ подтвердил наличие trigger_mark в кадре, мы считаем этот кадр «Золотым».
Нарезка по сетке: Только в этот момент мы «режем» этот же самый кадр по твоим координатам LINE_X и отправляем 10 ячеек в ИИ одним пакетом (batch).


Триггер на весь кадр: Сначала ищем trigger_mark (анализ всей картинки).
Цикл по ячейкам: Если триггер сработал, мы проходим по ячейкам в цикле. Поскольку модель не принимает пакеты (batch), мы будем подавать их по одной,
но только один раз за проход ряда. Это всё равно в сотни раз быстрее, чем мучить ИИ в каждом кадре.
Защита консоли: Добавлен блок try-except, чтобы при любой ошибке ты видел текст, а консоль ждала нажатия клавиши.

Давай "проявим" зрение ИИ
Я добавил в код визуальные подсказки. Теперь прямо в окне над каждой ячейкой будет написано то, что ИИ видит в данный момент. 
Это позволит тебе настроить линии LINE_X идеально под таблетки.


Триггер на весь кадр: ИИ сначала ищет trigger_mark на общем изображении.
Золотой кадр: Анализ 10 ячеек происходит только в момент срабатывания триггера.
Пакетная обработка (фикс): Поскольку твой ONNX не принимает пакет из 10 фото одновременно, я сделал быстрый цикл, который прогоняет 10 ячеек единоразово за один проход ряда.
Пропуск пустоты: Индексы настроены так, чтобы игнорировать зону между 6 и 7 линией (индекс 5).
Защита консоли: Блок try-except не даст окну закрыться при ошибке.

можешь тут исправить детекцию золотого ряда по всему кадру из ИИ класса trigger_mark

и один раз детекцию ячеек 10 зон между 6 и 7 пустая зона ее не учитывай больше ничего не делай

ИИ YOLO классы
empty
tablet_g_no = g_no
tablet_g_od = g_od
tablet_g_ok = g_ok
tablet_w_no = w_no
tablet_w_od = w_od
tablet_w_ok = w_ok
trigger_mark_yes
trigger_mark_no

классы ищи ряды по полному кадру
trigger_mark_yes - есть ряд
trigger_mark_no - нет ряда
Delay через сколько времени искать новый ряд в миллисекундах

алгоритм такой:

ии модель работает, не работает только счетчик
используем класс trigger_mark_no (между рядность) как основной триггер. Как только уверенность в том, что перед нами "положение между рядами", 
падает (значит, заехал ряд trigger_mark_yes), мы прибавляем ROWS +1 и ждем задержку Delay(...s) и так по кругу, считаем ряды


добавь новый ползунок пользователю     CONF_THRESHOLD = 0.80 для изменения, но чтобы окно видео камеры не изменилось и было оригинальному потоку


is_yes = (p_yes >= Conf) AND (p_yes - p_no >= Margin)