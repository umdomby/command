под кнопкой TRAIN AI MODEL  нужно вывести конец пути (папки - если вмещается выводи полный путь если нет сокращай начало), чтобы пользователь видел название папки - чтобы понимал от куда он будет создавать ИИ модель

под кнопкой ВЫБРАТЬ .ONNX МОДЕЛЬ нужно вывести конец пути (папки - если вмещается выводи полный путь если нет сокращай начало), чтобы пользователь видел название папки - чтобы понимал какая модель загружена
и название папкок в модель/tran/выведи название папок которые содержатся тут чтобы пользователь видел название классов для ИИ

в PanelAIModel.cs нужно добавить кнопку - создать ИИ модель, при нажатии на нее мы должны указать место и название модели. 
Мы указываем место и название - создается папка с таким названием и подпапки 

-train 
--train\normal
--train\anomaly

-val
--val\normal
--val\anomaly

после этого показывается папка и ее название
под ней текстовый путь к папке если не вмещается доходит до конца ширины окна то сделай отступ и троеточие
(окна растягиваются так что пользователю лучше чтобы путь был максимально показан до края окна)

кнопочку раскрыть + и -
при раскрытии покзываются папки и подпапки
-train
--train\normal
--train\anomaly

-val
--val\normal
--val\anomaly

и количество элементов
при нажати на подпапку открывается то место где лежат фото.


так же к ИИ модели нужно добавить к подпапкам классы дай инпут, где пользователь вводит название класса нажимает создать
и создаются сразу две папки в -train -val

-train
--train\normal
--train\anomaly
--train\new_folder

-val
--val\normal
--val\anomaly
--train\new_folder

«Выбрать .ONNX модель» — это для готовой модели (inference).
«Создать новую ИИ модель» — это для создания структуры папок под будущее обучение.
А кнопка TRAIN AI MODEL должна быть отдельной и чётко понимать, где лежат данные для обучения.
добавь кнопку у TRAIN AI MODEL - эта кнопка должна выбрать путь по которому будет создаваться ИИ модель и выведи ее путь для пользователя если вмещается выводи полный путь если нет сокращай начало
возле каждой папки (класса добавь возможность удалять - с подтверждением) - как пользователь подтвердил удаление класса он удаляется в  -train и -val


неправильная папка для обучения модели, нужно сделать в той же линии горизонтально с Создать новую ИИ модель , во второй половине выбрать ИИ модель, для дальнейшего TRAIN AI MODEL
эти пути должны сохраняться в config_default.json и вообще все пути должны сохраняться и изменяться, и загружаться из файла для пользователя, последние внесенные пути


public class PanelAIModel : DockContent когда мы создали или выбрали ИИ модель, нужно вывести папки и подпапки - и напротив каждой папки которые находятся train и val (в основном это anomaly и normal) сделать снимок для ИИ модели, чтобы потом обучить и чтобы снимок сохранялся оригинальному ищображению, так же добавь возможность сохранять выделенную область в кадре и сохранять его в новый класс. В этот класс сохраняется выделенная область, и в последствии можно при нажатии этого класса выделяется область на экране (где по этим координатам будет инспектор работать с ИИ моделью), сохраняй выделенную область как класс и пояснения в data.yaml где название класса выбранной зоны сохранялось класс и его координаты.
когда мы выбираем .ONNX МОДЕЛЬ ДЛЯ ИНСПЕКЦИИ подтягивается этот файл где мы ищем инспекцию по зонам.


