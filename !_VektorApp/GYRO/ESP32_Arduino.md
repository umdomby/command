Send(0,   200);   // прямо быстро
Send(0,100);   // прямо средне
Send(0,    50);   // медленно вперёд
Send(0,     0);   // стоп
Send(0,   -150);  // назад

Send(-150, 150);  // плавный поворот влево
Send( 150, 150);  // плавный поворот вправо

Send(-250, 0);    // разворот на месте влево
Send( 250, 0);    // разворот на месте вправо

показания от колес есть, я кручу колеса данные идут, колеса крутятся с усилием,
кнопку на гироскутере отлючаю данные перестают идти и колеса крутятся легче комманды типа Send(100,100) в консоль, двигатели не крутятся


Hoverboard smart balance 10 классических платах от TaoTao 
я уже программирую, порошиваю через ST Link v2, идут данные от мотор колес: скорость, напряжение...
но они у меня никак не хотят крутиться
дай ссылку где проект управляется через SERIAL (у меня SERIAL3), готовый бинарник для STM и ардуинка которая задает сорость моторам.
и самое главное схема подключения какие пины от ардуино идут на плату к RX, TX, GND


Убедись, что стоит VLT_MODE (это главное)
Найди строку:
C#define CTRL_MOD_REQ    SPD_MODE
Обязательно замени на:
C#define CTRL_MOD_REQ    VLT_MODE


Найди:
#define I_MOT_MAX       21              // [A] Maximum single motor current limit
Поменяй на:
#define I_MOT_MAX       15


так данные идут, в твоем коде данные не идут, можно в настройках перед бинарником настроить 
чтобы выводили данные еще в сериалпорт чтобы посмотреть плата принимает командны на вращение моторов и почему не вращает?