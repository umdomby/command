ESP32-S3 Hoverboard
управлять двумя проводами (RX + TX) + GND.
Правильное подключение:
Вариант A (рекомендуемый) — USART2 (левый датчик)

GPIO17TX    (STM32 → ESP)
GPIO16RX    (ESP → STM32)
GND         GND